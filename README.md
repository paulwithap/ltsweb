## Requirements

    the `www-node` project should be in `/var/www/www-node`

## Installation

    git clone git@github.com:paulwithap/ltsweb.git
    cd ltsweb
    sudo npm install . -g

## Usage

    ltsweb [start|stop]
